// fr.cu is intentionally minimal as all Fr field arithmetic is implemented via inline __host__ __device__ functions in fr.cuh to ensure performance and avoid CUDA linkage overhead.

#include "fr.cuh"
